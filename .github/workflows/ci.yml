name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-test-deploy:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest ]
        qt-version: [ '6.5.0' ]
        include:
          - os: ubuntu-latest
            build-dir: build
            executable: AdvancedTicTacToe
            artifact_name: AdvancedTicTacToe-linux
          - os: windows-latest
            build-dir: build/Release
            executable: AdvancedTicTacToe.exe
            artifact_name: AdvancedTicTacToe-windows

    steps:
      # ===== SETUP =====
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: ${{ matrix.qt-version }}
          cached: true
          setup-python: false  # Avoid conflicts with system Python

      - name: Install dependencies
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            sudo apt-get update
            sudo apt-get install -y build-essential zip
          else
            choco install cmake 7zip -y
          fi

      # ===== BUILD & TEST =====
      - name: Configure CMake
        shell: bash
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release -DENABLE_TESTS=ON

      - name: Build project
        shell: bash
        run: cmake --build build --config Release

      - name: Verify executable exists
        shell: bash
        run: |
          echo "Build directory contents:"
          ls ${{ matrix.build-dir }}
          if [ ! -f "${{ matrix.build-dir }}/${{ matrix.executable }}" ]; then
            echo "Executable not found!"
            exit 1
          fi

      # ===== ARTIFACT PREPARATION =====
      - name: Prepare artifact
        shell: bash
        run: |
          mkdir -p package
          cp "${{ matrix.build-dir }}/${{ matrix.executable }}" package/
          
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            cp -r resources package/
            zip -r ${{ matrix.artifact_name }}.zip package/
          else
            # Ensure windeployqt can find Qt binaries
            export PATH="/opt/qt/${{ matrix.qt-version }}/msvc2019_64/bin:$PATH"
            
            # Deploy Qt dependencies (skip translations if they fail)
            if windeployqt package/${{ matrix.executable }} --release --no-translations; then
              echo "Qt deployment succeeded"
            else
              echo "⚠️ Qt deployment had issues, continuing anyway"
            fi
            
            # Copy DLLs and compress
            cp "${{ matrix.build-dir }}"/*.dll package/
            7z a ${{ matrix.artifact_name }}.zip package/*
          fi

      # ===== ARTIFACT UPLOAD (Recommended) =====
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}.zip
